import styled from "styled-components";

const WrapperLoad = styled.div`
    background-color: var(--color-main);

    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;

    .spinner-border {
        color: var(--color-primary);
    }
`;

export default WrapperLoad;